const router=require('express').Router(); const c=require('./usuario.controller'); const {auth}=require('../../middlewares/auth.middleware'); const {requireManager}=require('../../shared/profile.service'); const {requireModuleAccess}=require('../../middlewares/module-access.middleware');
router.get('/',auth,c.list); router.post('/',auth,requireModuleAccess('administracao'),requireManager,c.create); router.put('/:id',auth,requireModuleAccess('administracao'),requireManager,c.update); router.delete('/:id',auth,requireModuleAccess('administracao'),requireManager,c.deactivate); module.exports=router;
